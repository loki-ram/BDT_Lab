# QuickCommerce Analytics - Dependencies
# Core
pandas>=2.0
numpy>=1.24
pyarrow>=14.0

# Spark (local mode for dev; Databricks has its own runtime)
pyspark>=3.5

# ML (included with pyspark, but listed for clarity)
# pyspark[ml] is bundled

# Dashboard
streamlit>=1.35
plotly>=5.18

# Voice
deepgram-sdk>=3.0
gTTS>=2.5

# AWS (for S3 access in production)
boto3>=1.34

# Utilities
scikit-learn>=1.3
